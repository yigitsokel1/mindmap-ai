# Legacy UI Components (Inactive Runtime)

This folder contains legacy UI components preserved for migration/reference only.

- Runtime status: inactive by default
- Active UI runtime: `frontend/app/components/` with semantic query flow

Do not wire new product behavior to components in this folder.

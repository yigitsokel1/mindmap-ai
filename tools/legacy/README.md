# Legacy Tools Quarantine

This directory contains legacy diagnostic and migration scripts that are not part of the active semantic runtime.

- `run_ingest.py`: legacy ingestion helper.
- `run_query.py`: legacy query helper.

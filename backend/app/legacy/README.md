# Legacy Runtime (Inactive)

This package contains legacy runtime code paths kept only for backward compatibility and migration support.

- Runtime status: inactive by default
- Active query runtime: `backend/app/services/query/` and `/api/query/semantic`
- Active graph read runtime: `/api/graph/semantic`

Do not add new production features here.

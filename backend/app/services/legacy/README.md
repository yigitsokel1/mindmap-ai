# Legacy Service Shims (Inactive Runtime)

This folder contains compatibility shims and legacy-only service access points.

- Runtime status: inactive by default
- Purpose: import compatibility during migration
- Removal: scheduled after migration completion (see per-file removal markers)

Do not introduce new runtime logic in this folder.
